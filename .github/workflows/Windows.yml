name: Windows

on: push

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build Debug.
      run: build.bat

    - name: Build Release.
      run: buildRelease.bat
